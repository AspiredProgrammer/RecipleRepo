import React from "react";
import './styles.scss';

const SearchBar = () => {
  return (
    <div className="SearchBar">
      <input></input>
      <button>Search</button>
    </div>
  );
};

export default SearchBar;
